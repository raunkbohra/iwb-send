{
  "framework": "nextjs",
  "buildCommand": "npm install -g pnpm && pnpm install --filter=@iwb/dashboard... && pnpm --filter=@iwb/dashboard run build",
  "installCommand": "npm install -g pnpm && cd ../.. && pnpm install",
  "env": {
    "NEXT_PUBLIC_API_URL": "@next-public-api-url"
  }
}
