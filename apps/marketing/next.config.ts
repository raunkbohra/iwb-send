import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Can export to static HTML later, or use ISR for dynamic content
  // output: 'export',
};

export default nextConfig;
